import React from "react";

export default function LandingPage({onPageChange}) {
    return (
        <section id="home" className="fixed inset-0 bg-[linear-gradient(to_right,#121d37,#1f2a3c,transparent),linear-gradient(to_bottom,#23344c,#2c314e)]">

        </section>
    );
}